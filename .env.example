# Copy to .env — only needed if you want the RunPod GPU engine.
# The app works fully on local CPU without this file.

RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=

# Max seconds to wait for one clip's transcription (covers GPU cold starts).
RUNPOD_JOB_TIMEOUT=600
